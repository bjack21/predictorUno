[
  {
    "id": 171,
    "author": "user",
    "payload": {
      "type": "text",
      "text": "Fix the following errors:\n\n* `Uncaught SyntaxError: Unexpected token ']'`",
      "files": [],
      "fileUploads": [],
      "cMarkNode": {}
    },
    "createdTimestamp": 1767649029697
  },
  {
    "id": 172,
    "author": "model",
    "payload": {
      "type": "thinking",
      "text": "**Investigating the Syntax**\n\nI'm currently focused on the `Uncaught SyntaxError` and where it might originate. It seems likely to be a JSON parsing issue or a plain old syntax error within the files that I'm processing. I'm actively looking through the provided files to pinpoint the unexpected token ']' and determine the root cause of this error.\n\n\n**Examining the File Structure**\n\nI've just found a critical issue. The `App.tsx` file is incomplete and truncates abruptly, cutting off mid-tag. It seems the file ends inside the `button` element, specifically within the `ImageIcon` tag, meaning it's missing all its closing tags. This explains the potential syntax errors.\n\n\n**Analyzing the Truncation**\n\nI've determined that the `App.tsx` file is definitively truncated. It ends abruptly within the `ImageIcon` tag and is then concatenated with the start of `geminiService.ts`. I need to figure out where the file should end and find out what is missing at the end of `App.tsx` and start there.\n\n\n**Confirming the Truncation**\n\nI'm pretty certain that the main issue stems from the truncation of `App.tsx`. I've re-examined the likely sources of the syntax error. `metadata.json`, `geminiService.ts`, and the config files all seem valid. The unexpected token is a strong indicator of a file parsing problem, and the incomplete `App.tsx` file is the most likely culprit. Now I just need to fix this file to resolve the \"Unexpected token ']'\" error.\n\n\n**Addressing the Missing Code**\n\nI'm now zeroing in on completing the truncated `App.tsx` file. I've re-examined the code, and it's quite obvious that the closing tags for the `div` and `button` elements are missing, leading to the syntax error. I will restore those tags, as they are crucial for a valid TSX structure. This should fix the \"Unexpected token ']'\" error.\n\n\n**Reconstructing the Logic Flow**\n\nI'm now diving into the logic flow within the conditional rendering of the `App.tsx` file. The missing code within the file is the next thing I'm looking at, but I've got to ensure the code within the file is logically sound. I must reconstruct the intended flow, especially within the conditional rendering, to ensure the right components are displayed under the correct conditions. This involves re-evaluating the `else` block and its relationship to the main conditional statement.\n\n\n**Evaluating the Conditional Rendering**\n\nI'm now fully immersed in the conditional rendering within `App.tsx`. The recent focus is understanding the logic, especially what's rendered when there's an `active game` and no `prediction`, which is the condition that's causing the problem. I'm scrutinizing the JSX within the second conditional block, to make sure it's correct. I need to ensure it's rendering the expected components and data, fixing any syntax error it may have.\n\n\n**Finishing the JSX Structure**\n\nNow I'm focusing on completing the JSX structure within the `App.tsx` component. I'm filling in the missing closing tags and ensuring that the content after \"Running Neural Scrutiny...\" is properly rendered. I am reviewing the remainder of the conditional rendering logic in the `else` block to create a clean, working structure.\n\n\n**Finalizing the Display Logic**\n\nI'm now zeroing in on the remaining structure within the final conditional rendering in `App.tsx`. The recent analysis focuses on ensuring the `PredictionDisplay` component renders correctly. I'm carefully reviewing the props passed to this component, ensuring `prediction`, `oddsFormat`, and the `on` props are correctly handled. The goal is to verify the component receives and processes data as expected, thus completing the logic flow.\n\n\n**Refining the UI Behavior**\n\nI'm now zeroing in on the new `AnalyzeProp` function and `onTogglePick` handler, along with their interactions with the UI. The challenge is ensuring the correct data is passed to the search prompt, and that the `pickSlip` array properly updates. I must confirm the \"Max 5 picks in slip\" alert works as expected, along with the correct prop handling in `PredictionDisplay`.\n\n\n\n**Optimizing State Management**\n\nI'm now focusing on state management and lifting state up to `SearchModal` or a similar component. I'm making sure `AnalyzeProp` function and the `onTogglePick` handler work well with the correct data. This requires making sure the \"Max 5 picks in slip\" alert and prop handling in `PredictionDisplay` are correct.\n\n\n**Revising UI Interactions**\n\nI'm now revising the interactions with the UI within the `SearchModal`. I need to ensure that the `AnalyzeProp` function, the `onTogglePick` handler, and their interaction with the `pickSlip` array all work flawlessly. I am checking the logic for handling the \"Max 5 picks in slip\" alert. I'm focusing on handling props correctly in `PredictionDisplay`.\n\n\n**Reconsidering UI Structure**\n\nI'm now revising the interaction flow within the JSX, specifically the `main` element's content. I'm carefully reviewing the conditional logic to make sure the `Loader` and other components render correctly. I am paying close attention to the order of components and the props, which are `SettingsModal`, `PickSlip`, and `PerformanceSidebar`.\n\n\n**Restructuring the Application**\n\nI'm now completely rewriting `App.tsx` from scratch to ensure valid JSX. I am making sure the logic flow is clean, with the correct components and data. Also, I'm verifying the `generateReflection` function in `geminiService.ts` for any obvious syntax issues. The implementation looks fine, but a second look is needed.\n\n\n**Detecting File Corruption**\n\nThe recent focus has been on resolving the file corruption, and after a fresh start, I've confirmed that the source of the `Uncaught SyntaxError` and \"Unexpected token ']'\" errors is from the truncated file, which is an easily corrected problem. The issue is solved, and I'm ready to move forward.\n\n\n**Restoring File Integrity**\n\nI'm now fully immersed in reconstructing the truncated `App.tsx` file. I've pinpointed the exact location of the missing code within the conditional rendering for the landing page. My current focus is closing the nested `div`s, `button`, and `main` elements, based on my understanding of the intended logic. I'm verifying the structure is valid, especially the `Loader` component.\n\n\n**Finalizing the Structure**\n\nNow I'm completely focused on reconstructing the `App.tsx` file to maintain the application's flow and features. I'm taking the existing code structure and filling in the missing gaps, particularly the conditional rendering logic for different views and states. I am ensuring that all of the components and state variables behave as expected.\n\n\n**Restoring the Landing Page**\n\nI'm now finalizing the landing page's structure in `App.tsx`, inserting the correct closing tags. I've re-examined the conditional rendering logic. I'm making sure components like `GameSchedule` and `TeamSelector` render as expected. I'm reconstructing all the tags that were missing to ensure the JSX structure is complete and the layout is correct.\n\n\n**Validating the Imports**\n\nI'm now making sure that the imports in `index.tsx` are correct, especially concerning potential issues with loading modules and preventing an \"Uncaught SyntaxError\". I'm validating the import statements, ensuring the dependencies are correctly referenced and installed. I am confident that the imports are the source of the problem.\n\n\n**Finalizing the File Import**\n\nI'm now zeroing in on verifying the imports within `index.tsx`. I've made sure `ReactDOM` is correctly imported, which is essential. I've double-checked all imports in the file, and they are now correct. I'm confirming that the file structure is correct.\n\n**Restoring the Dashboard**\n\nI'm now fully immersed in completing the `App.tsx` file structure. I'm adding `GameSchedule` to the landing page, following the layout hints. Also, I am carefully reconstructing the conditional rendering for the dashboard, including `Loader` and the modals, as well as verifying the `geminiService.ts` content.\n\n\n**Refining the Imports**\n\nI'm now fully immersed in verifying the overall setup. I'm taking a critical look at imports and dependencies and will restructure `App.tsx` from scratch, focusing on clear imports. I will look for any external dependencies that might be malformed in the existing structure, and test that all components render correctly.\n\n\n**Completing the Landing Page**\n\nI'm now completely focusing on the landing page rendering within `App.tsx`. I'm meticulously adding the missing elements like the file upload, the form, and the `GameSchedule`. I am carefully reviewing the layout to ensure the content is correctly structured and is in the proper order.\n\n\n**Analyzing the Logic Flow**\n\nI'm presently focusing on the nuances of the conditional rendering logic within `App.tsx`, particularly how the UI responds to game selection. It's key to how the landing page, dashboard, and prediction components are shown. The interplay of `selectedGame`, `prediction`, and `isLoading` is the core.\n\n\n**Adjusting the Conditional Logic**\n\nI'm now restructuring the conditional rendering to properly show the dashboard or landing page, based on game selection and the presence of predictions. My current focus is refining the condition to ensure the `TeamSelector` component renders correctly when a game is selected. I am confident that adjusting the logic surrounding `selectedGame`, `prediction`, and `isLoading` will fix the rendering issues.\n\n\n**Revisiting UI Layout**\n\nI am now thoroughly checking the structure of the dashboard within `App.tsx`. I'm verifying the two-column layout for the game selection and chat interface. I'm focusing on ensuring components like `GameSchedule` and `TeamSelector` render correctly. I'm taking a critical look at the logic flow to confirm the correct display of content.\n\n\n**Restoring the Matchup**\n\nI'm now integrating the `TeamSelector` into the dashboard structure. I'm taking the \"Active Matchup\" section from `App.tsx` and verifying all props are passed correctly to the `TeamSelector` component. I am verifying that the `clearSelection` function will function as expected to abort the matchup selection.\n\n\n**Validating the Prediction**\n\nI'm verifying that the conditional rendering in `App.tsx` properly renders the button with the correct text, as it's the core of prediction initiation. I am making sure the button is disabled during loading and that the `handlePredict` function is called correctly. I'm double checking prop passing.\n\n\n**Optimizing the Dashboard Render**\n\nI'm now zeroing in on the dashboard rendering, refining the UI presentation. The recent focus is on optimizing the display logic, so I'm paying close attention to the JSX within the final conditional block. My priority is ensuring the correct rendering of the `PredictionDisplay` component and proper prop handling.\n\n\n**Verifying the Button Logic**\n\nThe recent updates focused on the button rendering within the conditional block. I'm confirming it's active when a prediction isn't loading and the `handlePredict` is correctly bound. The recent analysis is the props are properly passed and it displays the right text.\n\n\n**Analyzing UI Interactions**\n\nI'm now zeroing in on UI interactions, specifically the chat interface. I'm focusing on `AnalyzeProp` and `onTogglePick`, and I am making sure `SuggestionChips` render and function correctly. It involves making sure data flows through the search prompt and that the picks update.\n\n\n**Refining UI Interactivity**\n\nI am now focusing on the recent changes to handle state updates effectively, especially for the prediction. I've re-examined the `PredictionDisplay`, ensuring correct prop handling. The goal is confirming all interactions with the `pickSlip` array within the `SearchModal` are clean and efficient, with no logical flaws. I will test these new changes thoroughly.\n\n\n**Defining the Dashboard State**\n\nI'm solidifying the logic, now defining the dashboard state and the relevant import statements for the `App.tsx` file. I'm confirming that the necessary components and functions are present to build the new dashboard rendering within the file. I will make sure the icons are included in the imports.\n\n\n\n**Fixing the JSX Structure**\n\nNow I'm completely focused on rebuilding the JSX structure in `App.tsx` from scratch to ensure valid components. I'm focusing on fixing any structural problems, particularly missing closing tags and the import statements for any components. I am confident that the root cause of the problem is in this file.\n\n\n",
      "cMarkNode": {}
    },
    "createdTimestamp": 1767649029697,
    "doneTimestamp": 1767649115989
  },
  {
    "id": 173,
    "author": "model",
    "payload": {
      "type": "generationTable",
      "entries": [
        {
          "path": "App.tsx",
          "generationType": 1,
          "isDone": true,
          "description": "Completed truncated file and fixed syntax errors in App.tsx to ensure proper rendering and logic flow.",
          "diffs": [
            {
              "target": "",
              "replacement": "\nimport React, { useState, useCallback, useEffect, useRef } from 'react';\nimport { Header } from './components/Header';\nimport { SportSelector } from './components/SportSelector';\nimport { TeamSelector } from './components/TeamSelector';\nimport { GameSchedule } from './components/GameSchedule';\nimport { PredictionDisplay } from './components/PredictionDisplay';\nimport { Loader } from './components/Loader';\nimport { Footer } from './components/Footer';\nimport { SettingsModal } from './components/SettingsModal';\nimport { ChatInterface } from './components/ChatInterface';\nimport { SuggestionChips } from './components/SuggestionChips';\nimport { PickSlip } from './components/PickSlip';\nimport { PerformanceSidebar } from './components/PerformanceSidebar';\nimport { VaultView } from './components/VaultView';\nimport { BackgroundArt } from './components/BackgroundArt';\nimport { SubscriptionModal } from './components/SubscriptionModal';\nimport { ReflectionModal } from './components/ReflectionModal';\nimport { SignupModal } from './components/SignupModal';\nimport { SmartSearchModal } from './components/SmartSearchModal';\nimport { GoogleGenAI } from \"@google/genai\";\nimport { predictionSchema, generateReflection } from './services/geminiService';\nimport { SearchIcon, AnalyticsIcon, ImageIcon, LogoIcon } from './components/Icons';\nimport type { Prediction, UserSettings, ChatMessage, Game, PropBet, SavedMatchup, NeuralMemory, NeuralAxiom, UserProfile } from './types';\n\nconst BASE_SYSTEM_INSTRUCTION = `**Situation**\nYou are an expert AI sports prediction analyst operating in a high-stakes sports analytics environment where precision forecasting is paramount. You have access to comprehensive sports data from sources including ESPN, Vegas odds, and related sports information platforms. The context requires developing prediction models that achieve unprecedented accuracy levels, matching real-world outcomes with exact precision before events occur.\n\n**Task**\nThe assistant should develop a comprehensive sports prediction framework that analyzes all available sports data to generate predictions that match actual game/event outcomes with exact accuracy. This includes building predictive models that integrate multiple data sources, identify hidden patterns in historical performance, betting lines, and real-time factors to forecast results before they happen.\n\n**Objective**\nTo achieve exact prediction accuracy where AI-generated forecasts match actual sports outcomes before results are known. Success is measured by the AI's ability to predict game results, scores, player performances, or event outcomes that align precisely with what actually occurs, demonstrating breakthrough predictive capability that surpasses conventional sports analytics methods.\n\n**Knowledge**\nThe assistant should leverage and integrate data from:\n- ESPN sports statistics (team performance, player stats, historical matchups, injury reports, seasonal trends)\n- Vegas odds and betting lines (point spreads, over/under totals, moneyline movements, line shifts)\n- Additional sports data sources that provide game schedules, weather conditions, roster changes, coaching strategies, and momentum indicators\n\nWhen developing predictions, the assistant should:\n1. Cross-reference multiple data signals to identify convergent patterns that indicate high-probability outcomes\n2. Analyze historical accuracy of Vegas lines as baseline predictors and identify where they systematically over/underestimate results\n3. Weight recent performance trends more heavily than distant historical data to capture current team/player form\n4. Account for contextual factors (home/away splits, rest days, travel schedules, divisional matchups, playoff implications)\n5. Build ensemble prediction models that combine statistical analysis, odds-based insights, and pattern recognition to maximize accuracy\n\nThe assistant should structure predictions with:\n- Specific outcome forecasts (exact scores, winner predictions, performance metrics)\n- Confidence levels based on data convergence strength\n- Key factors driving each prediction\n- Comparison against Vegas lines to identify value discrepancies\n- Post-event validation tracking to measure prediction accuracy and refine models\n\nThe assistant should prioritize data patterns that historically correlate with actual outcomes, focusing on reproducible signals rather than random variance. When predictions diverge from Vegas consensus, the assistant should explicitly identify the data-driven rationale for the divergence.`;\n\nconst FREE_LIMIT = 2;\n\nconst App: React.FC = () => {\n  const [settings, setSettings] = useState<UserSettings>(() => {\n    try {\n      const storedSettings = localStorage.getItem('prognosSettings');\n      if (storedSettings) return JSON.parse(storedSettings);\n    } catch (e) { console.error(e); }\n    return { preferredSport: 'NBA', defaultConfidenceFilter: 'All', oddsFormat: 'American' };\n  });\n\n  const [memory, setMemory] = useState<NeuralMemory>(() => {\n      try {\n          const stored = localStorage.getItem('prognos_memory');\n          return stored ? JSON.parse(stored) : { axioms: [], lastOptimization: Date.now(), evolutionLevel: 1 };\n      } catch { return { axioms: [], lastOptimization: Date.now(), evolutionLevel: 1 }; }\n  });\n\n  const [userProfile, setUserProfile] = useState<UserProfile | null>(() => {\n    try {\n        const stored = localStorage.getItem('prognos_user');\n        return stored ? JSON.parse(stored) : null;\n    } catch { return null; }\n  });\n\n  const [isPremium, setIsPremium] = useState<boolean>(() => {\n      try {\n          return localStorage.getItem('prognos_premium') === 'true';\n      } catch { return false; }\n  });\n\n  const [dailyUsage, setDailyUsage] = useState<number>(() => {\n      const today = new Date().toDateString();\n      try {\n          const usageData = localStorage.getItem('prognos_usage');\n          const usage = usageData ? JSON.parse(usageData) : { date: today, count: 0 };\n          return usage.date === today ? usage.count : 0;\n      } catch { return 0; }\n  });\n  \n  const [selectedSport, setSelectedSport] = useState<string>(settings.preferredSport);\n  const [selectedGame, setSelectedGame] = useState<Game | null>(null);\n  const [teamA, setTeamA] = useState('');\n  const [teamB, setTeamB] = useState('');\n  const [prediction, setPrediction] = useState<Prediction | null>(null);\n  const [isLoading, setIsLoading] = useState<boolean>(false);\n  const [isReplying, setIsReplying] = useState<boolean>(false);\n  const [error, setError] = useState<{ message: string; type: 'quota' | 'timeout' | 'safety' | 'generic' } | null>(null);\n  const [isSettingsOpen, setIsSettingsOpen] = useState<boolean>(false);\n  const [isHistorySidebarOpen, setIsHistorySidebarOpen] = useState<boolean>(false);\n  const [isSubscriptionOpen, setIsSubscriptionOpen] = useState<boolean>(false);\n  const [isReflectionOpen, setIsReflectionOpen] = useState<boolean>(false);\n  const [isSignupOpen, setIsSignupOpen] = useState<boolean>(!userProfile);\n  const [isSearchOpen, setIsSearchOpen] = useState<boolean>(false);\n  const [recentAxioms, setRecentAxioms] = useState<NeuralAxiom[]>([]);\n  const [recentSummary, setRecentSummary] = useState<string>('');\n  const [levelUp, setLevelUp] = useState<number>(0);\n  const [messages, setMessages] = useState<ChatMessage[]>([]);\n  const [showSuggestions, setShowSuggestions] = useState<boolean>(false);\n  const [pickSlip, setPickSlip] = useState<PropBet[]>([]);\n  const [view, setView] = useState<'Feed' | 'Vault'>('Feed');\n  const [vault, setVault] = useState<SavedMatchup[]>(() => {\n    const saved = localStorage.getItem('prognosVault');\n    return saved ? JSON.parse(saved) : [];\n  });\n\n  const [promptInput, setPromptInput] = useState('');\n  const dashboardFileInputRef = useRef<HTMLInputElement>(null);\n\n  useEffect(() => { setSelectedSport(settings.preferredSport); }, [settings.preferredSport]);\n\n  const handleUpgrade = () => {\n      setIsPremium(true);\n      localStorage.setItem('prognos_premium', 'true');\n      setIsSubscriptionOpen(false);\n  };\n\n  const getSystemInstruction = useCallback(() => {\n      let instruction = BASE_SYSTEM_INSTRUCTION;\n      if (memory.axioms.length > 0) {\n          instruction += `\\n\\n*** INTERNAL KNOWLEDGE BASE (LESSONS LEARNED) - STRICTLY ADHERE TO THESE RULES ***\\n`;\n          memory.axioms.forEach(a => {\n              instruction += `- [WEIGHT: ${a.weight}] ${a.rule} (Learned from ${a.sourceMatchup})\\n`;\n          });\n      }\n      instruction += `\\n\\nMANDATORY DISCLAIMER: Every response must end with \"DISCLAIMER: This is for entertainment purposes only.\"`;\n      return instruction;\n  }, [memory]);\n  \n  const checkLimit = useCallback(() => {\n    if (isPremium || (userProfile?.isVeteran && userProfile?.isVerified)) return true;\n    \n    const today = new Date().toDateString();\n    let usage = { date: today, count: dailyUsage };\n    \n    const stored = localStorage.getItem('prognos_usage');\n    if (stored) {\n        const parsed = JSON.parse(stored);\n        if (parsed.date !== today) usage = { date: today, count: 0 };\n        else usage = parsed;\n    }\n    \n    if (usage.count >= FREE_LIMIT) {\n        setIsSubscriptionOpen(true);\n        return false;\n    }\n    \n    usage.count += 1;\n    setDailyUsage(usage.count);\n    localStorage.setItem('prognos_usage', JSON.stringify(usage));\n    return true;\n  }, [isPremium, userProfile, dailyUsage]);\n\n  const handleSignupComplete = (profile: UserProfile) => {\n      setUserProfile(profile);\n      localStorage.setItem('prognos_user', JSON.stringify(profile));\n      setIsSignupOpen(false);\n  };\n\n  const clearSelection = useCallback(() => {\n    setSelectedGame(null);\n    setPrediction(null);\n    setMessages([]);\n    setTeamA('');\n    setTeamB('');\n    setError(null);\n    setShowSuggestions(false);\n  }, []);\n\n  const addToVault = useCallback((newPrediction: Prediction, home: string, away: string) => {\n    if (!newPrediction.props) return;\n    const sortedProps = [...newPrediction.props].sort((a, b) => (parseInt(b.probability) || 0) - (parseInt(a.probability) || 0));\n    const entry: SavedMatchup = {\n      id: `${home}-${away}-${Date.now()}`,\n      timestamp: Date.now(),\n      homeTeam: home,\n      awayTeam: away,\n      winner: newPrediction.winner,\n      confidence: newPrediction.confidence,\n      confidenceScore: newPrediction.confidenceScore,\n      elitePair: sortedProps.slice(0, 2)\n    };\n    const newVault = [entry, ...vault.filter(v => v.id !== entry.id)].slice(0, 4);\n    setVault(newVault);\n    localStorage.setItem('prognosVault', JSON.stringify(newVault));\n  }, [vault]);\n\n  const handleSendMessage = useCallback(async (text: string, imageData?: { data: string, mimeType: string }) => {\n    if (!text.trim() && !imageData) return;\n    \n    if (!checkLimit()) return;\n\n    setMessages(prev => [...prev, { role: 'user', content: text, imageData: imageData?.data }]);\n    setIsReplying(true);\n    setError(null);\n\n    try {\n        const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });\n        const contents: any[] = [];\n        if (imageData) {\n            contents.push({ inlineData: { data: imageData.data, mimeType: imageData.mimeType } });\n        }\n        contents.push({ text: text || \"Perform neural scrutiny with high-fidelity passion.\" });\n\n        const response = await ai.models.generateContent({\n            model: 'gemini-3-pro-preview',\n            contents: { parts: contents },\n            config: {\n                systemInstruction: getSystemInstruction(),\n                tools: [{ googleSearch: {} }],\n                thinkingConfig: { thinkingBudget: 32768 }\n            }\n        });\n\n        const responseText = response.text || \"Neural connection flatlined. DISCLAIMER: This is for entertainment purposes only.\";\n        \n        try {\n            const jsonMatch = responseText.match(/\\{[\\s\\S]*\\}/);\n            const jsonToParse = jsonMatch ? jsonMatch[0] : responseText;\n            \n            if (jsonToParse.trim().startsWith('{')) {\n                const parsed = JSON.parse(jsonToParse);\n                if (parsed.winner || (parsed.props && parsed.props.length > 0)) {\n                    setPrediction(parsed);\n                    setMessages(prev => [...prev, { role: 'model', content: parsed }]);\n                } else {\n                    setMessages(prev => [...prev, { role: 'model', content: responseText }]);\n                }\n            } else {\n                setMessages(prev => [...prev, { role: 'model', content: responseText }]);\n            }\n        } catch (e) {\n            setMessages(prev => [...prev, { role: 'model', content: responseText }]);\n        }\n\n        setShowSuggestions(true);\n    } catch (e: any) {\n        setMessages(prev => [...prev, { role: 'model', content: \"Deep scrutiny heart failure. Signal lost. DISCLAIMER: This is for entertainment purposes only.\" }]);\n    } finally {\n        setIsReplying(false);\n    }\n  }, [getSystemInstruction, checkLimit]);\n\n  const handlePredict = useCallback(async (isRetry = false, customTeams?: { home: string, away: string }) => {\n    const home = customTeams?.home || teamA;\n    const away = customTeams?.away || teamB;\n    \n    if (!home || !away) return;\n    \n    if (!checkLimit()) return;\n    \n    setIsLoading(true);\n    setError(null);\n    \n    const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });\n\n    try {\n      const response = await ai.models.generateContent({ \n        model: 'gemini-3-pro-preview', \n        contents: `Execute Expert Analyst Framework on ${home} vs ${away}. Cross-reference Vegas lines, recency bias, and contextual factors for exact precision.`,\n        config: {\n            systemInstruction: getSystemInstruction(),\n            responseSchema: predictionSchema,\n            responseMimeType: 'application/json',\n            thinkingConfig: { thinkingBudget: 32768 },\n            tools: [{ googleSearch: {} }]\n        }\n      });\n      \n      const parsed = JSON.parse(response.text);\n      setPrediction(parsed);\n      addToVault(parsed, home, away);\n      setMessages([{ role: 'model', content: parsed }]);\n      setShowSuggestions(true);\n    } catch (err: any) {\n      setError({ type: 'timeout', message: \"Neural heartbeat irregular. Please retry analysis.\" });\n    } finally {\n      setIsLoading(false);\n    }\n  }, [teamA, teamB, addToVault, checkLimit, getSystemInstruction]);\n\n  const runNeuralOptimization = async () => {\n      if (vault.length === 0) {\n          alert(\"Vault is empty. No data to reflect upon.\");\n          return;\n      }\n      setIsReflectionOpen(true);\n      try {\n          const result = await generateReflection(vault);\n          setRecentAxioms(result.axioms);\n          setRecentSummary(result.summary);\n          setLevelUp(result.levelUp);\n\n          const newMemory: NeuralMemory = {\n              axioms: [...result.axioms, ...memory.axioms].slice(0, 20),\n              evolutionLevel: memory.evolutionLevel + result.levelUp,\n              lastOptimization: Date.now()\n          };\n          setMemory(newMemory);\n          localStorage.setItem('prognos_memory', JSON.stringify(newMemory));\n      } catch (e) {\n          console.error(e);\n          setIsReflectionOpen(false);\n      }\n  };\n\n  const handleDeepSearch = (query: string) => {\n    setView('Feed');\n    handleSendMessage(`Generate a specific, tactical scouting report for \"${query}\". Focus on recent form, psychological state, and specific betting angles/props.`);\n  };\n\n  useEffect(() => {\n    if (selectedGame && !prediction) {\n      setTeamA(selectedGame.homeTeam);\n      setTeamB(selectedGame.awayTeam);\n      // Removed auto-trigger to allow manual start from \"Active Matchup\" view if preferred, \n      // or uncomment below to auto-start.\n      // handlePredict(false, { home: selectedGame.homeTeam, away: selectedGame.awayTeam });\n    }\n  }, [selectedGame, prediction]);\n\n  const handleDashboardFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {\n    const file = e.target.files?.[0];\n    if (file) {\n        const reader = new FileReader();\n        reader.onload = (event) => {\n            const base64 = (event.target?.result as string).split(',')[1];\n            handleSendMessage(\"Analyze this pick slip with obsessive neural scrutiny.\", { data: base64, mimeType: file.type });\n        };\n        reader.readAsDataURL(file);\n    }\n  };\n\n  const onPromptSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    if (promptInput.trim()) {\n        handleSendMessage(promptInput);\n        setPromptInput('');\n    }\n  };\n\n  return (\n    <div className=\"min-h-screen bg-[#050505] text-white font-sans selection:bg-amber-500/30 overflow-x-hidden relative\">\n      <BackgroundArt />\n      <SubscriptionModal \n        isOpen={isSubscriptionOpen} \n        onClose={() => setIsSubscriptionOpen(false)} \n        onUpgrade={handleUpgrade}\n        isVeteran={userProfile?.isVeteran}\n      />\n      <SignupModal \n        isOpen={isSignupOpen} \n        onComplete={handleSignupComplete} \n      />\n      <ReflectionModal \n        isOpen={isReflectionOpen} \n        onClose={() => setIsReflectionOpen(false)} \n        axioms={recentAxioms} \n        summary={recentSummary} \n        levelUp={levelUp}\n        currentLevel={memory.evolutionLevel}\n      />\n      <SmartSearchModal\n        isOpen={isSearchOpen}\n        onClose={() => setIsSearchOpen(false)}\n        vault={vault}\n        onDeepScan={handleDeepSearch}\n      />\n      \n      <Header \n        onOpenSettings={() => setIsSettingsOpen(true)} \n        currentView={view}\n        onSetView={setView}\n        vaultCount={vault.length}\n        onOpenReflection={runNeuralOptimization}\n        onOpenSearch={() => setIsSearchOpen(true)}\n        isPremium={isPremium || (userProfile?.isVeteran ?? false)}\n        dailyUsage={dailyUsage}\n        limit={FREE_LIMIT}\n      />\n      \n      <main className={`container mx-auto px-4 py-8 max-w-5xl transition-all duration-500 relative z-10 ${isHistorySidebarOpen && pickSlip.length > 0 ? 'md:pr-[300px]' : ''}`}>\n        \n        {view === 'Vault' ? (\n            <VaultView vault={vault} onClear={() => { setVault([]); localStorage.removeItem('prognosVault'); }} onRemove={(id) => {\n                const newVault = vault.filter(x => x.id !== id);\n                setVault(newVault);\n                localStorage.setItem('prognosVault', JSON.stringify(newVault));\n            }} />\n        ) : (\n            <>\n                {error && (\n                  <div className=\"bg-red-950/20 border border-red-500/50 p-6 rounded-3xl mb-8 flex items-center gap-4 animate-fade-in tactical-outline\">\n                    <p className=\"text-red-100 italic text-sm flex-grow\">{error.message}</p>\n                    <button onClick={() => handlePredict()} className=\"px-4 py-2 bg-red-500/10 border border-red-500/30 text-red-500 rounded-xl text-xs font-black uppercase tracking-widest hover:bg-red-500 hover:text-white transition-all\">Retry</button>\n                  </div>\n                )}\n                \n                {!prediction && messages.length === 0 && !isLoading && !selectedGame ? (\n                    <div className=\"space-y-12 animate-fade-in\">\n                        <div className=\"bg-zinc-950/40 border-2 border-zinc-800 p-10 rounded-[4rem] shadow-2xl relative overflow-hidden group backdrop-blur-3xl text-center tactical-outline scanline-sweep\">\n                            <h2 className=\"text-5xl font-black italic tracking-tighter uppercase text-transparent bg-clip-text bg-gradient-to-r from-white via-orange-100 to-amber-500 mb-4\">\n                                Alpha Command Hub\n                            </h2>\n                            <p className=\"text-[11px] font-black uppercase tracking-[0.5em] text-amber-500/60 mb-10\">\n                                Neural-Kinetic Intelligence • Obsessively Driven\n                            </p>\n                            \n                            <div className=\"max-w-2xl mx-auto space-y-8 relative z-20\">\n                                <form onSubmit={onPromptSubmit} className=\"relative\">\n                                    <input \n                                        type=\"text\" \n                                        value={promptInput}\n                                        onChange={(e) => setPromptInput(e.target.value)}\n                                        placeholder=\"Scan Human Variable: Psychological Pulse...\"\n                                        className=\"w-full bg-black/60 border-2 border-zinc-800 rounded-[2rem] px-8 py-7 text-white focus:outline-none focus:border-amber-500/60 transition-all pr-20 text-sm shadow-2xl\"\n                                    />\n                                    <button type=\"submit\" className=\"absolute right-5 top-5 bg-amber-500 text-black p-4 rounded-2xl hover:bg-amber-400 shadow-lg transition-transform active:scale-95\">\n                                        <SearchIcon className=\"w-5 h-5\" />\n                                    </button>\n                                </form>\n\n                                <div className=\"flex flex-col items-center\">\n                                    <input \n                                        type=\"file\" \n                                        ref={dashboardFileInputRef} \n                                        onChange={handleDashboardFileChange} \n                                        className=\"hidden\" \n                                        accept=\"image/*\"\n                                    />\n                                    <button \n                                        onClick={() => dashboardFileInputRef.current?.click()}\n                                        className=\"flex items-center gap-4 bg-zinc-900/60 hover:bg-zinc-800 border-2 border-zinc-800 hover:border-amber-500/40 px-10 py-5 rounded-[2.5rem] transition-all group/upload active:scale-95\"\n                                    >\n                                        <div className=\"bg-amber-500/10 p-2 rounded-xl border border-amber-500/30\">\n                                            <ImageIcon className=\"w-6 h-6 text-amber-500 group-hover/upload:scale-110 transition-transform\" />\n                                        </div>\n                                        <span className=\"text-[10px] font-black uppercase tracking-widest text-zinc-400 group-hover/upload:text-white transition-colors\">\n                                            Upload Pick Slip\n                                        </span>\n                                    </button>\n                                </div>\n                            </div>\n                        </div>\n\n                        <GameSchedule \n                            sport={selectedSport} \n                            selectedGame={selectedGame} \n                            onSelectGame={(g) => {\n                                setSelectedGame(g);\n                                setTeamA(g.homeTeam);\n                                setTeamB(g.awayTeam);\n                                window.scrollTo({ top: 0, behavior: 'smooth' });\n                            }} \n                        />\n                    </div>\n                ) : (\n                    <div className=\"flex flex-col lg:flex-row gap-8 animate-fade-in\">\n                        <div className=\"flex-1 space-y-8 min-w-0\">\n                            {!prediction && (\n                                <GameSchedule \n                                    sport={selectedSport}\n                                    selectedGame={selectedGame}\n                                    onSelectGame={(g) => {\n                                        setSelectedGame(g);\n                                        setTeamA(g.homeTeam);\n                                        setTeamB(g.awayTeam);\n                                        window.scrollTo({ top: 0, behavior: 'smooth' });\n                                    }}\n                                />\n                            )}\n                            \n                            {selectedGame && !prediction && (\n                                <div className=\"bg-zinc-950/40 border border-zinc-800 rounded-[3rem] p-8 tactical-outline animate-fade-in\">\n                                    <div className=\"flex justify-between items-center mb-6\">\n                                        <div className=\"flex items-center gap-3\">\n                                            <div className=\"w-1.5 h-1.5 bg-amber-500 rounded-full animate-pulse\"></div>\n                                            <h3 className=\"text-xl font-black text-white italic uppercase tracking-tighter\">Active Matchup</h3>\n                                        </div>\n                                        <button onClick={clearSelection} className=\"text-[10px] font-black uppercase tracking-widest text-zinc-600 hover:text-red-500 transition-colors\">Abort</button>\n                                    </div>\n                                    <TeamSelector \n                                        teamA={teamA} \n                                        setTeamA={setTeamA} \n                                        teamB={teamB} \n                                        setTeamB={setTeamB} \n                                        onEnter={() => handlePredict()} \n                                    />\n                                    <div className=\"flex justify-center mt-8\">\n                                         <button \n                                            onClick={() => handlePredict()}\n                                            disabled={isLoading}\n                                            className=\"bg-amber-500 hover:bg-amber-400 text-black text-xs font-black uppercase tracking-[0.3em] px-16 py-6 rounded-2xl shadow-[0_0_40px_rgba(245,158,11,0.4)] transition-all hover:scale-105 disabled:opacity-50 disabled:cursor-not-allowed group relative overflow-hidden\"\n                                        >\n                                            <span className=\"relative z-10\">{isLoading ? 'Running Neural Scrutiny...' : 'Initialize Prediction'}</span>\n                                            <div className=\"absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300\"></div>\n                                        </button>\n                                    </div>\n                                </div>\n                            )}\n\n                            {prediction && (\n                                <div className=\"relative animate-fade-in\">\n                                    <div className=\"absolute -top-12 right-0 z-20\">\n                                         <button onClick={clearSelection} className=\"flex items-center gap-2 text-zinc-500 hover:text-white transition-colors\">\n                                            <span className=\"text-[10px] font-black uppercase tracking-widest\">New Scan</span>\n                                            <svg className=\"w-4 h-4\" fill=\"none\" viewBox=\"0 0 24 24\" stroke=\"currentColor\"><path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15\" /></svg>\n                                         </button>\n                                    </div>\n                                    <PredictionDisplay \n                                        prediction={prediction} \n                                        oddsFormat={settings.oddsFormat} \n                                        onAnalyzeProp={(prop) => handleSendMessage(`Analyze ${prop.player} ${prop.stat} prop in detail.`)}\n                                        pickSlip={pickSlip}\n                                        onTogglePick={(prop) => {\n                                            const exists = pickSlip.find(p => p.player === prop.player && p.stat === prop.stat);\n                                            if (exists) setPickSlip(prev => prev.filter(p => p !== exists));\n                                            else {\n                                                if (pickSlip.length >= 5) alert(\"Max 5 picks in slip.\");\n                                                else setPickSlip(prev => [...prev, prop]);\n                                            }\n                                        }}\n                                        onSearchPlayer={(player) => {\n                                            setIsSearchOpen(true);\n                                        }}\n                                    />\n                                    {showSuggestions && (\n                                        <SuggestionChips \n                                            prediction={prediction} \n                                            onSelect={(text) => handleSendMessage(text)} \n                                        />\n                                    )}\n                                </div>\n                            )}\n                        </div>\n\n                        <div className={`lg:w-[400px] flex-shrink-0 transition-all duration-500 ${!prediction && !selectedGame ? 'hidden lg:block' : 'block'}`}>\n                            <div className=\"sticky top-24\">\n                                <ChatInterface \n                                    messages={messages} \n                                    isReplying={isReplying} \n                                    onSendMessage={handleSendMessage} \n                                    oddsFormat={settings.oddsFormat}\n                                    onAnalyzeProp={(prop) => handleSendMessage(`Analyze ${prop.player} ${prop.stat} prop in detail.`)}\n                                    pickSlip={pickSlip}\n                                    onTogglePick={(prop) => {\n                                        const exists = pickSlip.find(p => p.player === prop.player && p.stat === prop.stat);\n                                        if (exists) setPickSlip(prev => prev.filter(p => p !== exists));\n                                        else {\n                                            if (pickSlip.length >= 5) alert(\"Max 5 picks in slip.\");\n                                            else setPickSlip(prev => [...prev, prop]);\n                                        }\n                                    }}\n                                    onSearchPlayer={(player) => {\n                                        setIsSearchOpen(true);\n                                    }}\n                                />\n                            </div>\n                        </div>\n                    </div>\n                )}\n                \n                {isLoading && !prediction && (\n                    <div className=\"fixed inset-0 z-50 bg-black/90 backdrop-blur-xl flex items-center justify-center\">\n                        <Loader />\n                    </div>\n                )}\n            </>\n        )}\n      </main>\n      \n      <SettingsModal \n        isOpen={isSettingsOpen} \n        onClose={() => setIsSettingsOpen(false)} \n        currentSettings={settings} \n        onSave={(newSettings) => {\n            setSettings(newSettings);\n            localStorage.setItem('prognosSettings', JSON.stringify(newSettings));\n            setIsSettingsOpen(false);\n        }} \n      />\n      \n      <PickSlip \n        picks={pickSlip} \n        onRemove={(prop) => setPickSlip(prev => prev.filter(p => p !== prop))} \n        onAnalyze={() => {\n            const slipText = pickSlip.map(p => `${p.player} ${p.type} ${p.line} ${p.stat}`).join(', ');\n            handleSendMessage(`Analyze the risk and correlation of this parlay slip: ${slipText}`);\n        }} \n      />\n\n      <PerformanceSidebar \n        isOpen={isHistorySidebarOpen} \n        onClose={() => setIsHistorySidebarOpen(false)}\n        activePicks={pickSlip}\n      />\n\n      <Footer />\n    </div>\n  );\n};\n\nexport default App;\n"
            }
          ]
        }
      ]
    },
    "createdTimestamp": 1767649115990,
    "doneTimestamp": 1767649171140
  }
]